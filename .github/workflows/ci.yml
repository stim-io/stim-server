name: ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  baseline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify cold-start baseline files exist
        run: |
          test -f README.md
          test -f AGENTS.md
          test -f .gitignore
